package com.krayirhan.benimformum.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Tasarım sistemindeki sabit yüzey ve sınır tonları (Material [Color] ile birlikte).
 */
object FormColors {
    val background = LightBackground
    val surface = LightSurface
    val softSurface = LightSurfaceVariant
    val primarySoftSurface = FormHeroCardFill
    val border = LightOutline
    val primary = SteelPrimary
    val textPrimary = LightOnBackground
    val textSecondary = LightOnSurfaceVariant
}
